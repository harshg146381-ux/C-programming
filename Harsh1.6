#include <stdio.h>

int main() {
    int hours, minutes;

    // Taking input from user
    printf("Enter time in hours: ");
    scanf("%d", &hours);

    // Conversion
    minutes = hours * 60;

    // Display result
    printf("%d hours = %d minutes\n", hours, minutes);

    return 0;
}
